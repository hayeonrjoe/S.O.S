<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sos.signal.admin.mapper.UserMapper">
    <insert id="insertUser">
        INSERT INTO admin
            (a_name, a_contact, a_email, a_pw, a_admin_type, a_company_name, a_company_contact, a_company_address)
        VALUES
            (#{name}, #{contact}, #{email}, #{pw}, #{admin_type}, #{company_name}, #{company_contact}, #{company_address})
    </insert>

    <select id="getUserByEmail" resultType="com.sos.signal.admin.vo.UserVo">
        SELECT *
        FROM admin
        WHERE a_email = #{email}
    </select>

    <select id="getUserById" resultType="com.sos.signal.admin.vo.UserVo">
        SELECT *
        FROM admin
        WHERE a_id = #{id}
    </select>

    <delete id="deleteUser">
        DELETE
        FROM admin
        WHERE a_id = #{id}
    </delete>
</mapper>